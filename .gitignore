# O repositório computacaografica guarda só as atividades entregues.
# Tudo é ignorado por padrão e qualquer pasta atividadeN/ entra sozinha,
# sem precisar mexer neste arquivo a cada entrega.

/*

!.gitignore
!README.md
!/atividade*/

# nunca versionar dependências nem build
node_modules/
dist/
.DS_Store
